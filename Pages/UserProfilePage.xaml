<local:PageContent x:Class="Memenim.Pages.UserProfilePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:Memenim.Pages"
             xmlns:widgets ="clr-namespace:Memenim.Widgets"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding name}" Style="{DynamicResource Header1}"/>
        <Image Source="{Binding photo}" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="50" Height="200" Width="200" Stretch="Uniform"/>
        <!--<WrapPanel Grid.Row="1">
            <uc:UserProfileStat x:Name="wdgStatThink" StatTitle="Thought" StatValue="{Binding Path=think}"/>
        </WrapPanel>-->
    </Grid>
</local:PageContent>
