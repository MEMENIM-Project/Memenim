<UserControl x:Class="AnonymDesktopClient.Pages.PostOverlayPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:AnonymDesktopClient.Pages"
             xmlns:uc="clr-namespace:AnonymDesktopClient.Widgets"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Loaded="Grid_Loaded">
        <Button Click="Button_Click" Background="{DynamicResource Overlay.Background}" BorderThickness="0"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="50">
            <StackPanel>
                <uc:PostWidget x:Name="wdgPost"/>
                <uc:CommentsList x:Name="wdgCommentsList"/>
                <uc:WriteComment x:Name="wdgUserComment"/>
            </StackPanel>
        </ScrollViewer>


    </Grid>
</UserControl>
