<UserControl x:Class="Memenim.Pages.PlaceholderPage"
             d:DataContext="{d:DesignInstance local:PlaceholderPage}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Memenim.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Loaded="Grid_Loaded">
        <StackPanel>
            <TextBlock Style="{DynamicResource Header1}" Name="txtSmile" HorizontalAlignment="Center" Margin="5" FontWeight="Bold" FontSize="34"/>
            <TextBlock Text="{DynamicResource ResourceKey=WIPTitle}" Style="{DynamicResource Header2}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
