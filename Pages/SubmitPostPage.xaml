<UserControl x:Class="Memenim.Pages.SubmitPostPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:widgets="clr-namespace:Memenim.Widgets"
             xmlns:local="clr-namespace:Memenim.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Loaded="Grid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="{DynamicResource ResourceKey=SubmitPostTitle}" Style="{DynamicResource Header1}" Margin="16 5" Grid.Row="0" />
        <controls:MetroAnimatedTabControl Grid.Row="1" Margin="16 5" controls:TabControlHelper.Underlined="TabItems">
            <TabItem Header="{DynamicResource ResourceKey=EditTitle}">
                <ScrollViewer  VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <TextBox MinHeight="200" controls:TextBoxHelper.Watermark="{DynamicResource ResourceKey=PostContent}" TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding text}" Margin="0 5"/>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel>
                                <TextBlock Style="{DynamicResource Header2}" Text="{DynamicResource ResourceKey=ImageSourceTitle}" Margin="0 5"/>
                                <RadioButton Content="{DynamicResource ResourceKey=URLSelect}" Name="rbImageRaw" GroupName="ImageSource" Margin="0 5"/>
                                <RadioButton Content="{DynamicResource ResourceKey=TennorSelect}" Name="rbImageTennor" GroupName="ImageSource" Margin="0 5"/>
                                <RadioButton Content="{DynamicResource ResourceKey=GallerySelect}" Name="rbImageGallery" GroupName="ImageSource" Margin="0 5"/>
                            </StackPanel>
                            <Image x:Name="imgPreview" Height="100" Width="100" Margin="10"/>
                        </StackPanel>

                        <Button Click="SelectPhoto_Click" Content="{DynamicResource ResourceKey=SelectPhoto}" Style="{StaticResource MahApps.Styles.Button.Square}" Margin="0 5"/>
                        <!--<CheckBox IsChecked="{Binding hidden}">Show post</CheckBox>-->
                        <CheckBox IsChecked="{Binding open_comments}" Content="{DynamicResource ResourceKey=OpenComments}" Margin="0 5"/>
                        <CheckBox IsChecked="{Binding adult}" Margin="0 5">18+</CheckBox>
                        <CheckBox IsChecked="{Binding author_watch}" Content="{DynamicResource ResourceKey=PostAnon}" Margin="0 5"/>
                        <Button Click="Button_Click" Style="{StaticResource MahApps.Styles.Button.Square.Accent}" Content="{DynamicResource ResourceKey=SubmitPostButton}" Margin="0 5"/> 
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="{DynamicResource ResourceKey=PreviewTitle}">
                <widgets:PostWidget x:Name="wdgPostPreview"/>
            </TabItem>
        </controls:MetroAnimatedTabControl>
        

    </Grid>
</UserControl>
