<UserControl x:Class="AnonymDesktopClient.Widgets.WriteComment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:gif="http://wpfanimatedgif.codeplex.com"
             xmlns:local="clr-namespace:AnonymDesktopClient.Widgets"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="400">
    <Border Padding="5 0 5 0" BorderThickness="0">
    <Grid Background="{StaticResource MahApps.Brushes.Gray10}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>
        <Image gif:ImageBehavior.AnimatedSource="{Binding UserAvatarSource}"  Height="40" Width="40"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="10 5 10 5" Name="txtContent"
                 Controls:TextBoxHelper.ButtonCommand="{Binding AnononymCommandPress}"
                 Style="{DynamicResource MahApps.Styles.TextBox.Button}"/>
        <local:IconButton Grid.Column="2" IconKind="MessageSend" IconButtonClick="Button_Click" />
    </Grid>
    </Border>
</UserControl>
