<UserControl x:Class="AnonymDesktopClient.Widgets.NavigationPersistent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnonymDesktopClient.Widgets"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="40">
    <Grid Background="Blue">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>

        <Button></Button>
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <Button Name="btnNavHome" Click="btnNavHome_Click" Height="40"></Button>
        </StackPanel>
        <Button Grid.Row="2" Name="btnNavSettings" Click="btnNavSettings_Click"></Button>
    </Grid>
</UserControl>
