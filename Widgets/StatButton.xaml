<UserControl x:Class="AnonymDesktopClient.Widgets.StatButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:local="clr-namespace:AnonymDesktopClient.Widgets"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <Button Width="{Binding ButtonSize}" Height="{Binding ButtonSize}" Margin="5" Click="Button_Click" Style="{DynamicResource MahApps.Styles.Button.Circle}" >
            <iconPacks:PackIconModern Kind="{Binding IconType}"></iconPacks:PackIconModern>
        </Button>
        <TextBlock x:Name="tbValue" VerticalAlignment="Center" Foreground="{StaticResource MahApps.Brushes.Text}"></TextBlock>
    </StackPanel>
</UserControl>
