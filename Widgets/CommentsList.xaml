<UserControl x:Class="Memenim.Widgets.CommentsList"
             d:DataContext="{d:DesignInstance local:CommentsList}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Memenim.Widgets"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Padding="5" BorderThickness="0">
        <Grid Background="{StaticResource MahApps.Brushes.Gray10}" Loaded="Grid_Loaded">
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="40"/>
                <RowDefinition />
            </Grid.RowDefinitions>

            <TextBlock Style="{DynamicResource Header2}" Text="{DynamicResource ResourceKey=CommentsTitle}" Margin="16 5 0 0" Grid.Row="0" />
            <Button x:Name="btnLoadMore" Click="btnLoadMore_Click" Margin="16 5 16 5" Content="{DynamicResource ResourceKey=LoadComments}" Style="{StaticResource MahApps.Styles.Button.Square}" Grid.Row="1"/>
            <ItemsControl Grid.Row="2" Name="lstComments" Margin="16 5 16 5" HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Stretch">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
