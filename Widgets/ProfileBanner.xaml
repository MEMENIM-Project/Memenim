<UserControl x:Class="AnonymDesktopClient.Widgets.ProfileBanner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnonymDesktopClient.Widgets"
            xmlns:gif="http://wpfanimatedgif.codeplex.com"
             mc:Ignorable="d" d:DesignWidth="800" Height="215">
    <Grid Loaded="UserBanner_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0.4*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0.4*"/>
        </Grid.ColumnDefinitions>
        <Image gif:ImageBehavior.AnimatedSource="{Binding banner}" Grid.RowSpan="2" Grid.ColumnSpan="2" Stretch="UniformToFill">
            <Image.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Select GIF from Tenor" Click="SelectBannerFromTennor_Click"/>
                </ContextMenu>
            </Image.ContextMenu>
        </Image>
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Background="{StaticResource MahApps.Brushes.Badged.Background}" VerticalAlignment="Center" Height="61">
            <TextBlock Margin="5 3 0 0" Foreground="{StaticResource MahApps.Brushes.Text}" Text="{Binding name}"></TextBlock>
            <TextBlock Margin="5 3 0 0" Foreground="{StaticResource MahApps.Brushes.Text}" Text="{Binding login}"></TextBlock>
        </StackPanel>
        <Image Name="mediaAvatar" Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Stretch="Uniform" gif:ImageBehavior.AnimatedSource="{Binding photo}" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Image.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Select image from URL" Click="SelectAvatarFromUrl_Click"/>
                    <MenuItem Header="Select GIF from Tenor" Click="SelectAvatarFromTennor_Click"/>
                </ContextMenu>
            </Image.ContextMenu>
        </Image>
    </Grid>
</UserControl>
