<UserControl x:Class="Memenim.Widgets.UserStatus"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Memenim.Widgets"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock VerticalAlignment="Center">
            <TextBlock.Style>
                <Style>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=StatusValue}" Value="Active">
                            <Setter Property="TextBlock.Text" Value="Active"/>
                            <Setter Property="TextBlock.Foreground" Value="White"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=StatusValue}" Value="Banned">
                            <Setter Property="TextBlock.Text" Value="Banned"/>
                            <Setter Property="TextBlock.Foreground" Value="Red"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=StatusValue}" Value="Moderator">
                            <Setter Property="TextBlock.Text" Value="Banned"/>
                            <Setter Property="TextBlock.Foreground" Value="Violet"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=StatusValue}" Value="Violet">
                            <Setter Property="TextBlock.Text" Value="Banned"/>
                            <Setter Property="TextBlock.Foreground" Value="Red"/>
                        </DataTrigger>

                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
