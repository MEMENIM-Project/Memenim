<UserControl x:Class="Memenim.Widgets.UserComment"
             d:DataContext="{d:DesignInstance local:UserComment}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Memenim.Widgets"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400">

    <Border Padding="5">
        <Grid Loaded="Grid_Loaded">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="15"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>

            <Image Source="{Binding Path=CurrentCommentData.user.photo, Converter={StaticResource UserAvatarImageSourceConverter}}" Stretch="UniformToFill" Width="100" Height="100" Grid.Row="0" Grid.Column="0" Grid.RowSpan="3" MouseUp="Avatar_MouseUp" VerticalAlignment="Top"/>
            <TextBlock Text="{Binding Path=CurrentCommentData.user.name, Converter={StaticResource UserNameConverter}}" TextWrapping="Wrap" Grid.Row="0" Grid.Column="1" Style="{DynamicResource Header2}" VerticalAlignment="Bottom" Margin="5"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Path=CurrentCommentData.text}" TextWrapping="Wrap" Style="{DynamicResource Paragraph}"
                       Margin="5" VerticalAlignment="Top" HorizontalAlignment="Stretch" TextAlignment="Left"/>
            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <local:StatButton Icon="ThumbsUp" ButtonClick="Like_Click" ButtonSize="16"/>
                <local:StatButton Icon="ThumbsDown" ButtonClick="Dislike_Click" ButtonSize="16"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
