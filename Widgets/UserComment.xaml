<UserControl x:Class="Memenim.Widgets.UserComment"
             d:DataContext="{d:DesignInstance local:UserComment}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Memenim.Widgets"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400">

    <Border Padding="5">
        <Grid Loaded="Grid_Loaded">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="15"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>

            <Image Grid.Row="0" Grid.Column="0" Grid.RowSpan="3" Width="100" Height="100"
                   Source="{Binding Path=CurrentCommentData.user.photo, Converter={StaticResource UserAvatarImageSourceConverter}}"
                   Stretch="UniformToFill" MouseUp="Avatar_MouseUp" VerticalAlignment="Top"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Style="{DynamicResource Header2}"
                       Text="{Binding Path=CurrentCommentData.user.name, Converter={StaticResource UserNameConverter}}"
                       TextWrapping="Wrap" VerticalAlignment="Bottom" Margin="5"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Style="{DynamicResource Paragraph}"
                       Text="{Binding Path=CurrentCommentData.text}"
                       TextWrapping="Wrap" VerticalAlignment="Top" HorizontalAlignment="Stretch" TextAlignment="Left" Margin="5"/>
            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <local:StatButton ButtonSize="16" IconKind="ThumbsUp" ButtonClick="Like_Click"/>
                <local:StatButton ButtonSize="16" IconKind="ThumbsDown" ButtonClick="Dislike_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
