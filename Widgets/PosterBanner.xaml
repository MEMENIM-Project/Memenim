<UserControl x:Class="AnonymDesktopClient.Widgets.PosterBanner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnonymDesktopClient.Widgets"
             mc:Ignorable="d" 
             d:DesignHeight="136" d:DesignWidth="800">
    <Grid Loaded="Grid_Loaded" MouseLeftButtonUp="OnPoster_MouseUp">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Image Grid.Column="0" Source="/Resources/placeholder_avatar.jpg" Stretch="Uniform"/>
        <StackPanel Grid.Column="1" Margin="20 0 0 0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding PosterName}" Style="{DynamicResource Header2}" IsEnabled="{Binding IsAnonymousPost}"/>
                <TextBlock Text="Anonymous" Style="{DynamicResource Paragraph}" Name="txtAnon" VerticalAlignment="Bottom" Margin="5"/>
            </StackPanel>
            <TextBlock Text="{Binding PostTime}" Style="{DynamicResource Paragraph}"/>
        </StackPanel>
    </Grid>
        
</UserControl>
